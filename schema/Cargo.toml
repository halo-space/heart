[package]
name = "schema"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"
license = "MIT"

[lib]
name = "schema"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
